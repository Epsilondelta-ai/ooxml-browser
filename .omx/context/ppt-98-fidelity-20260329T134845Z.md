# Context Snapshot: PPT 98-percent fidelity push

- **Task statement:** Raise PPT evidence for `sample1` slide 1, `sample5` slide 2, and `sample6` slide 1 to at least 98%, ideally near 100%, using any render technology (SVG, Canvas, or hybrid) while keeping the work parser-grounded and semantic-first.
- **Desired outcome:** A concrete consensus plan and then sustained Ralph execution that upgrades the PPT render path toward scene-accurate output, minimizes sample-specific heuristics, and converges the target-slide screenshots plus vision verdicts to 98%+ evidence.
- **Known facts / evidence:**
  - The repo already completed a prior semantic-priority pass with commits `b9f3229`, `0cda1c2`, `c0d2a94`, `bef3b9c`, and `00f2d41`.
  - Latest accepted verification from that pass: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` all passed.
  - Latest cited target-slide scores from the previous pass reached roughly `sample1/1 85.56`, `sample5/2 81.35`, and `sample6/1 89.77`, with semantic improvement on sample6 despite slight RMSE noise.
  - Current PPT preview still relies on example-layer heuristics in `examples/basic/src/main.ts`, so 98%+ likely requires a stronger PPT scene renderer rather than incremental CSS tweaks alone.
  - Parser/model/render code is mainly under `packages/pptx/**`, `packages/render/**`, and `examples/basic/**`.
- **Constraints:**
  - Parser-grounded semantic fidelity outranks example styling.
  - SVG, Canvas, or hybrid rendering is allowed.
  - Evidence must be measured on `sample1/1`, `sample5/2`, `sample6/1` with screenshot + diff + vision review.
  - Example heuristics may only be removed after semantic replacements exist.
  - Work should proceed through ralplan then Ralph automatically to completion.
- **Unknowns / open questions:**
  - Whether 98%+ can be reached with SVG-only output or needs a Canvas pass.
  - Which remaining hotspots are dominated by text layout, geometry, fills/strokes, or residual preview heuristics.
  - What minimal renderer architecture shift is sufficient for 98% evidence without overbuilding the system.
- **Likely codebase touchpoints:**
  - `packages/pptx/src/parser.ts`
  - `packages/pptx/src/model.ts`
  - `packages/render/src/pptx.ts`
  - `examples/basic/src/main.ts`
  - `tools/generate-ppt-sample-screenshot-report.mjs`
  - `tests/pptx-*.test.ts`
  - `tests/render-and-browser.test.ts`
  - `.omx/plans/*ppt-98-fidelity*`
